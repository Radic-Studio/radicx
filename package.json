{
  "name": "radicx",
  "version": "0.5.0",
  "private": true,
  "description": "RadicX V1 NMCN Midwifery CBT preparation platform",
  "type": "module",
  "engines": {
    "node": ">=22 <23"
  },
  "packageManager": "npm@10.9.2",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node scripts/lint.mjs",
    "typecheck": "node scripts/typecheck.mjs",
    "test": "node --test tests/*.test.mjs",
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "a11y:smoke": "node scripts/a11y-smoke.mjs",
    "bundle:baseline": "node scripts/bundle-baseline.mjs",
    "db:start": "supabase start",
    "db:reset": "supabase db reset",
    "db:test": "supabase test db",
    "db:lint": "supabase db lint --local --level warning",
    "db:types": "supabase gen types typescript --local --schema public,storage > supabase/types/database.generated.ts",
    "verify": "npm run lint && npm run typecheck && npm test && npm run scan:secrets && npm run build && npm run a11y:smoke && npm run bundle:baseline"
  }
}
