# M2 Storage Access Controls

`question-media` is private and grants authenticated object retrieval without browser upload rights. `source-evidence` and `admin-uploads` have no browser policies. Protected source/admin content must be delivered later through trusted, audited workflows.
