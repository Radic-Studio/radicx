# M2 Hosted Provisioning Blocker

Approved hosted staging creation target: Radic Studio organization, `RadicX Staging`, London (`eu-west-2`), reported cost $0/month.

The Supabase management operation returned `ServiceUnavailableException` with a partial-system-outage notice on repeated creation attempts. Project listing after each failure returned no projects, so no partial project was created.

This blocker prevents hosted migration application, advisors and staging validation. It does not justify marking M2 accepted.
