# M2 Curriculum Foundation

The curriculum hierarchy is `programmes -> subjects -> topics`. Topics support arbitrary recursive nesting through `parent_topic_id`, with composite foreign-key integrity ensuring a child remains inside its subject. Taxonomy administration UI and content workflow are deferred to M4.
