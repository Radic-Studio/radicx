# M2 Question Revision Foundation

Published question revisions are durable historical records. Content fields are immutable after publication; quarantine/archive may change availability status without rewriting the content. Corrections create a new row/revision, while historical session answers continue referencing the exact prior `questions.id`.
