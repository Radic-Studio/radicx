# M2 AAL2 Foundation

The private staff-role helper can require JWT `aal2` before treating a staff assignment as authorized. Full admin MFA enforcement remains a later milestone, but M2 avoids designing staff authorization around user-editable metadata.
