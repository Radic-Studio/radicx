# M2 Staff Role Model

Authoritative roles are `content_editor`, `clinical_reviewer`, `item_reviewer`, `content_admin`, `support_admin`, and `super_admin`, stored only in `private.staff_roles`. User metadata is never authoritative for staff access.
