# M2 Google OAuth Readiness

Google OAuth is retained only as architecture readiness in M2. Client ID/secret configuration and end-user UI are not implemented in this milestone; provider secrets must remain environment-managed.
