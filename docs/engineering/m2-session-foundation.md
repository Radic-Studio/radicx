# M2 Session Foundation

`public.sessions` supports the approved modes `study`, `diagnostic`, `practice_mock`, and `realistic_exam`. `public.session_answers` stores the selected option, confidence, flag state, answer time and an idempotency `operation_id`. Full Study/Exam lifecycle, scoring, timer and offline synchronization behavior remain later milestones.
