# M2 Hosted Auth Checklist

When hosted staging is available, verify email/password is enabled, email confirmation is required, password recovery redirects are approved, phone signup remains disabled, TOTP/MFA readiness is available, and no Google OAuth/CAPTCHA/SMTP secret is stored in the repository. Production SMTP remains deferred.
