# M2 SMTP Boundary

Development Auth may use Supabase's development mail path. Production custom SMTP is explicitly deferred to the publishing milestone while the schema/auth design remains compatible with it.
