# M2 Question Randomization Foundation

Questions receive a bounded `random_bucket` value to support later efficient candidate-pool sampling by subject/topic/status without introducing full mock blueprint logic in M2.
