# M2 Profile Foundation

Each Supabase Auth user receives one `profiles` row through a trusted trigger. Students can read their own profile and update only `display_name`; ownership, timestamps and staff authority are not user-editable profile fields.
