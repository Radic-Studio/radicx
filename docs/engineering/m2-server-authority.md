# M2 Server Authority

Derived learning state (`user_progress`, `user_question_state`, `user_subject_stats`, `user_topic_stats`) is owner-readable but not directly writable by authenticated students. This preserves the project rule that correctness, readiness and other derived values are trusted/server-authoritative.
