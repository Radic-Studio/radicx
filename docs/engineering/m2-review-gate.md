# M2 Review Gate

This branch must not merge to `staging` until both the existing application verification workflow and the new database workflow pass. Hosted Supabase validation and advisor review remain required after CI and before milestone acceptance.
